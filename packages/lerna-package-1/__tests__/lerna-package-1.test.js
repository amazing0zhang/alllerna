'use strict';

const lernaPackage1 = require('..');
const assert = require('assert').strict;

assert.strictEqual(lernaPackage1(), 'Hello from lernaPackage1');
console.info("lernaPackage1 tests passed");

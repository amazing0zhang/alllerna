'use strict';

const lernaPackage2 = require('..');
const assert = require('assert').strict;

assert.strictEqual(lernaPackage2(), 'Hello from lernaPackage2');
console.info("lernaPackage2 tests passed");

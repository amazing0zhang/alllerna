'use strict';

const lernaPackage3 = require('..');
const assert = require('assert').strict;

assert.strictEqual(lernaPackage3(), 'Hello from lernaPackage3');
console.info("lernaPackage3 tests passed");
